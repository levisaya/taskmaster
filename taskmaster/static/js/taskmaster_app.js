// Generated by CoffeeScript 1.8.0
(function() {
  var taskmasterApp;

  taskmasterApp = angular.module('taskmasterApp', []);

  taskmasterApp.controller('ProcessListController', function($scope, $http) {
    $scope.process_list_model = {};
    $scope.last_status_time = 0.0;
    $scope.update_model = function(data) {
      var model_data, process_index, _ref;
      _ref = data.process_data;
      for (process_index in _ref) {
        model_data = _ref[process_index];
        $scope.process_list_model[process_index] = model_data;
      }
      $scope.last_status_time = data.last_update_time;
      return $scope.reschedule_status_update();
    };
    $scope.reschedule_status_update = function() {
      return $http.get('process_status/' + $scope.last_status_time.toFixed(2)).success($scope.update_model).error($scope.reschedule_status_update);
    };
    $scope.reschedule_status_update();
  });

}).call(this);
